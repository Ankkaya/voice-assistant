# Voice Assistant 开发约定

## App 打包与 Release 更新内容

- 每次执行 App 打包或发布前，必须检查自上一个 Release 标签以来的提交记录、实际变更文件及功能行为，确认本次版本包含的用户可感知变化。
- Release 的“更新内容”必须根据本次提交文件对应的实际功能整理，包括新增功能、体验优化和问题修复；不得凭空编写，也不得遗漏已经随安装包交付的重要变化。
- 更新内容应使用简洁、面向用户的中文逐条描述，说明“新增了什么”或“修复了什么”，避免只罗列提交哈希、文件名或内部实现细节。
- 不得使用 GitHub 自动生成的 `Full Changelog` 链接作为唯一更新内容；可以保留该链接作为补充，但前面必须有完整的功能更新说明。
- 纯重构、测试或构建脚本调整如果不影响用户体验，可以不写入用户侧更新内容；若影响安装、兼容性、权限或数据，则必须说明。
- 打包前需要核对 App 版本号、Release 标签、安装包文件名和更新内容是否对应同一版本。
- 如果无法从提交和变更文件确定某项功能是否应对用户公开，必须先向用户确认，不得直接发布含糊或不准确的更新说明。
